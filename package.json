{
  "name": "anticss",
  "version": "1.0.19",
  "description": "A class-less CSS framework for prototyping web-apps.",
  "author": "Sebastian Alsina",
  "main": "anti.min.css",
  "license": "MIT",
  "scripts": {
    "push": "git add . && git commit -am \"Auto Deploy\" && git push",
    "minify": "uglifycss anti.css > anti.min.css",
    "deploy": "yarn run minify && yarn run push && yarn publish"
  },
  "devDependencies": {
    "uglifycss": "^0.0.29"
  }
}
